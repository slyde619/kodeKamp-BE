# GitHub User Fetcher

A simple async JavaScript utility that fetches and returns GitHub user details by username via the GitHub API.

# KCStore - Product Management System

A simple JavaScript class-based store that supports adding, viewing, updating, and deleting products.
